SELECT ENAME,SAL,TO_CHAR(HIREDATE,'DD-MM-YYYY')FROM EMPLOYE38;
SELECT ENAME,SAL,HIREDATE ,TO_CHAR(HIREDATE,'DD-MM-YYYY')AS TOCHAR FROM EMPLOYE38;
SELECT ENAME,SAL,TO_DATE(SAL,'DD-MM-YYYY')FROM EMPLOYE38;
